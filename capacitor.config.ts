import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.micromoneymate.app',
  appName: 'Micro Money Mate',
  webDir: 'dist'
};

export default config;
